ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"

brew install \
       aria2 curl youtube-dl wget \
       chruby ruby-install \
       gist git hub \
       heroku-toolbelt \
       imagemagick \
       node yarn \
       mysql postgresql \
       reattach-to-user-namespace tmux \
       redis \
       s3cmd \
       ssh-copy-id \
       taglib \
       the_silver_searcher tree vim ctags \
       zsh zplug \
       erlang exenv elixir-build

brew install weechat --with-curl --with-lua --with-perl --with-python --with-ruby

brew tap neovim/neovim
brew install --HEAD neovim

brew cask install \
  google-chrome \
  dropbox \
  hammerspoon
