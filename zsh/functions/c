# Quickly navigate to a project - with autosuggest
c() {
  local dir="$HOME/dev"
  if [[ $# == 0 ]]; then
    cd $dir/$(find $dir -type d -maxdepth 3 | sed -E "s#^$dir##"| fzf)
  else
    cd $dir/$1;
  fi
}
_c() { _files -W ~/dev -/; }
compdef _c c

