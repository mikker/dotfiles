---
description: Launch a task in a new worktree and Fut workspace
argument-hint: "<task>"
---
Load and follow the `new-thread` skill to launch and orchestrate this task in a
dedicated wt worktree, a new Fut workspace rooted at that worktree, and a Pi
agent running as the workspace's initial terminal.

Treat all text below as the user's free-form task:

$ARGUMENTS
